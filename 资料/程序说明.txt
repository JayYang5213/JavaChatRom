 
你首先将服务器端的ChatServer.java编译通过,并运行起来,等待客户的呼叫.然后运行客户端程序ShangXian.

本项目使用套接字技术编写一个聊天室。

服务器为每个客户启动一个线程,在该线程中通过套接字和客户交流信息,
当你向服务器发送一条聊天信息 ”大家好”时,服务器要让所有的这些线程中的输出流写入信息 ”大家好”,
这样所有的客户的套接字的输入流就都读取到了这一条信息.如果你想把信息 ”你好”送给特定的用户(用户号为数字）
服务器就让特定的线程中的输出流写入信息 ”你好”,那么只有特定的客户的套接字的输入流可以读取到这条信息.
 
